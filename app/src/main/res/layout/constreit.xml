<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    android:layout_gravity="center">

    <!-- Franja Verde -->
    <View
        android:id="@+id/greenStripe"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:background="#008000"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/whiteStripe"
        app:layout_constraintWidth_percent="0.33" />

    <!-- Franja Blanca -->
    <View
        android:id="@+id/whiteStripe"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:background="#FFFFFF"
        app:layout_constraintStart_toEndOf="@id/greenStripe"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/redStripe"
        app:layout_constraintWidth_percent="0.34" />

    <!-- Franja Roja -->
    <View
        android:id="@+id/redStripe"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:background="#FF0000"
        app:layout_constraintStart_toEndOf="@id/whiteStripe"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.33" />

</androidx.constraintlayout.widget.ConstraintLayout>
